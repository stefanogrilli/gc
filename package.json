{
  "name": "gc",
  "version": "1.0.0",
  "description": "Google AI API Integration",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp generate.js dist/",
    "start": "node generate.js"
  },
  "dependencies": {
    "@google-ai/generativelanguage": "^1.1.0",
    "google-auth-library": "^9.0.0"
  }
}
